import { AboutStUsds } from '@/modules/ui/components/AboutStUsds';

// This will include all the cards for the expert modules
export function AdvancedAbout() {
  return <AboutStUsds isOverview={true} />;
}
